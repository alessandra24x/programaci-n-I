int countEmployees(Employee* list, int len);
int totalSalary(Employee* list, int len);
float averageSalary(int countEmp, float total);
int countEmployeesMoreAverageSalary(Employee* list, int len, float average);
void printSalaryInformation(Employee* list, int len);
